import React, { Component } from 'react';
import { HomeContainer, HomeTitle } from './styles';

export default class Home extends Component {
  render() {
    return (
      <HomeContainer>
        <HomeTitle>Rome scrin</HomeTitle>
      </HomeContainer>
    );
  }
}

