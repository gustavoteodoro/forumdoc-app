import React, { Component } from 'react';
import { Text } from 'react-native';
import { HomeContainer } from './styles';

export default class Calendar extends Component {
  render() {
    return (
      <HomeContainer>
        <Text>Calendar scrin</Text>
      </HomeContainer>
    );
  }
}

